<!DOCTYPE html>
<html>
<head>
  <title>Booking Details</title>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial; margin: 40px; background: #f4f4f4; }
    .container { max-width: 600px; margin: auto; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
    .info { margin: 10px 0; }
    .badge { padding: 5px 10px; border-radius: 4px; color: white; }
    .confirmed { background: #28a745; }
    .pending { background: #ffc107; color: black; }
    .cancelled { background: #dc3545; }
    button { padding: 8px 16px; margin: 5px; border: none; border-radius: 4px; cursor: pointer; }
    .edit { background: #ffc107; color: black; }
    .delete { background: #dc3545; color: white; }
    .back { display: inline-block; margin-top: 10px; color: #007bff; text-decoration: none; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Booking Details</h2>
    <div class="info"><strong>Name:</strong> <%= booking.customerName %></div>
    <div class="info"><strong>Phone:</strong> <%= booking.phone %></div>
    <div class="info"><strong>Date:</strong> <%= new Date(booking.date).toLocaleDateString() %></div>
    <div class="info"><strong>Time:</strong> <%= booking.time %></div>
    <div class="info"><strong>Pax:</strong> <%= booking.pax %></div>
    <div class="info"><strong>Status:</strong> 
      <span class="badge <%= booking.status %>"><%= booking.status %></span>
    </div>
    <% if (booking.notes) { %>
      <div class="info"><strong>Notes:</strong> <%= booking.notes %></div>
    <% } %>
    <% if (booking.photo) { %>
      <div class="info">
        <strong>Photo:</strong><br>
        <img src="data:<%= booking.photoMime %>;base64,<%= booking.photo %>" style="max-width:100%; border-radius:8px; margin-top:8px;">
      </div>
    <% } %>

    <div style="margin-top:20px;">
      <a href="/edit?_id=<%= booking._id %>"><button class="edit">Edit</button></a>
      <form action="/delete" method="POST" style="display:inline" onsubmit="return confirm('Delete this booking?')">
        <input type="hidden" name="_id" value="<%= booking._id %>">
        <button type="submit" class="delete">Delete</button>
      </form>
    </div>

    <a href="/" class="back">Back to List</a> | 
    <form action="/logout" method="POST" style="display:inline">
      <button type="submit" style="background:#6c757d; color:white; display:inline; width:auto; padding:5px 10px;">Logout</button>
    </form>
  </div>
</body>
</html>
