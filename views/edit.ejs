<!DOCTYPE html>
<html>
<head>
  <title>Edit Booking</title>
  <meta charset="utf-8">
  <style>
    body { font-family: Arial; margin: 40px; background: #f4f4f4; }
    .container { max-width: 600px; margin: auto; background: white; padding: 20px; border-radius: 8px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
    input, select, button { width: 100%; padding: 10px; margin: 8px 0; border: 1px solid #ccc; border-radius: 4px; }
    button { background: #ffc107; color: black; font-weight: bold; cursor: pointer; }
    button:hover { background: #e0a800; }
    .back { display: inline-block; margin-top: 10px; color: #007bff; text-decoration: none; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Edit Booking</h2>
    <form action="/update" method="POST" enctype="multipart/form-data">
      <input type="hidden" name="_id" value="<%= booking._id %>">
      <input type="text" name="customerName" value="<%= booking.customerName %>" required>
      <input type="tel" name="phone" value="<%= booking.phone %>" required>
      <input type="date" name="date" value="<%= booking.date.toISOString().split('T')[0] %>" required>
      <input type="time" name="time" value="<%= booking.time %>" required>
      <input type="number" name="pax" value="<%= booking.pax %>" min="1" required>
      <select name="status">
        <option value="confirmed" <%= booking.status === 'confirmed' ? 'selected' : '' %>>Confirmed</option>
        <option value="pending" <%= booking.status === 'pending' ? 'selected' : '' %>>Pending</option>
        <option value="cancelled" <%= booking.status === 'cancelled' ? 'selected' : '' %>>Cancelled</option>
      </select>
      <textarea name="notes" placeholder="Notes"><%= booking.notes || '' %></textarea>
      <% if (booking.photo) { %>
        <p>Current Photo:</p>
        <img src="data:<%= booking.photoMime %>;base64,<%= booking.photo %>" style="max-width:200px; border-radius:4px;">
      <% } %>
      <input type="file" name="photo" accept="image/*">
      <button type="submit">Update Booking</button>
    </form>
    <a href="/details?_id=<%= booking._id %>" class="back">Back to Details</a> | 
    <form action="/logout" method="POST" style="display:inline">
      <button type="submit" style="background:#dc3545; display:inline; width:auto; padding:5px 10px;">Logout</button>
    </form>
  </div>
</body>
</html>
